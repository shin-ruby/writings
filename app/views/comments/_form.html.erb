<%= simple_form_for([@writing, @writing.comments.build]) do |f| %>
  <%= f.input :commenter %>
  <%= f.input :user_id, as: :hidden, :input_html => { :value => current_user.id } %>
  <%= f.button :submit %>
<% end %>